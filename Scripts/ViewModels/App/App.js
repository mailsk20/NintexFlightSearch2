
/** @namespace */
var Nintex = function () {
    var baseUrl = '/';

    return {
        /**
         * Base URL used for resolving virtual apps or root sites
         * Update from server-side or hard-code in private property
         * Example: Emerio.baseUrl = '/myapp'
         */
        baseUrl: baseUrl,
    };
}();
